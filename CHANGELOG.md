# Changelog

## v1.2.2 — 2026-05-10 — contract job split + panel-content e2e smoke

- **Failure isolation restored.** `ci.yml`'s monolithic `contract` job is split into a per-suite `bats` matrix (`fail-fast: false`) plus a slimmed `contract` job for the non-bats surface (agent / command / hooks / template / plugin.json shape, end-to-end flows, drift lints, fingerprint detector). A flake in one bats file no longer blocks the others; re-runs scope to the failed slot only. TAP-line coverage parity verified pre/post: the two new panel-content cases land on top of the v1.2.1 baseline (was 437-case monolithic; now 439-case across the matrix). Closes the v1.1.1-deferred matrix-split item.
- **Panel-content e2e smoke.** New `tests/contract/panel-content.bats` (2-case file) exercises a single end-to-end flow — `kunskap init` → `kunskap config user` → `kunskap learn enable` → `kunskap learn status` — and asserts one stable phrase from each user-visible panel: the v1.1.1 `single-primary contract` framing on the init next-steps panel, and the all-TBD `ROLE-CHECK: no primary configured` copy on `learn status`. Distinct from the existing init-cli + learn-cli unit-style assertions: this is the only test that exercises the init template's TBD scaffold all the way through to the user-visible status output, catching wiring drift the unit suites can't see. Closes the v1.1.1-deferred panel-content e2e item.
- `bin/kunskap version` bumps to `1.2.2`. Plugin manifest version bumps to `1.2.2`.

## v1.2.1 — 2026-05-07 — wire orphan bats files into CI

- **Coverage gap closed.** Four bats files added in v1.1.0 — `sync-cli.bats`, `post-write-sync.bats`, `learn-enable-claudemd.bats`, `launch-footer-template.bats` — were never referenced from any `pN.yml` and slipped through v1.1.1's CI consolidation. Local `bats tests/contract/` ran them; CI didn't. Now wired into `ci.yml` (paired by feature: sync CLI + post-write-sync hook, then learn-enable + LAUNCH_FOOTER template — placed between the P6 recall step and the v1.2 setup steps). Local and CI suite counts now match; the previously-ungated 45-case coverage gap is closed. Pure CI hygiene — no behavioral changes to `bin/kunskap`, hooks, slash commands, or templates.
- `bin/kunskap version` bumps to `1.2.1`. Plugin manifest version bumps to `1.2.1`.

## v1.2.0 — 2026-05-06 — `/kunskap:setup` guided onboarding

- **`/kunskap:setup` slash command + `bin/kunskap setup` CLI verb.** Collapses what was 5–8 commands across CLI + slash + manual `_meta/roles.toml` editing into one short conversation. The slash command asks 3 questions (identity, vault path, solo-or-multiplayer), shows what it'll run, then composes the four underlying primitives — `kunskap config user`, `kunskap init`, the multiplayer `_meta/roles.toml` edit, and `kunskap learn enable` — in one go. End state: identity written, vault initialized, project marker in `.claude/kunskap.json`, capture + recall conventions injected into `CLAUDE.md`. Closes the v1.0.0 first-install friction (rank-2 real-usage finding from the post-v1.0 retro): the first end-to-end install took ~30 minutes and required deriving the per-project marker model from observed file presence; this PR's empirical smoke gets it under five.
- **Composition discipline (HARD CONTRACT):** `cmd_setup` calls existing CLI functions directly — `cmd_config_user`, `cmd_init`, `cmd_learn_enable`, `cmd_learn_status` — rather than reimplementing identity / marker / inject logic. Locked by `tests/contract/setup-cli.bats` "composition lock" case which greps the function body for the four delegated calls and asserts the absence of `inject_claudemd` / `marker_path` references in setup itself. Stops a future drive-by /simplify from collapsing the four steps into one inline bash invocation and losing the per-step exit-code clarity.
- **Slash-command-as-conversation, CLI-as-side-effects (HARD CONTRACT):** `commands/setup.md` declares `allowed-tools: Bash, Read` only — no `Write`, no `Edit`. The body's job is the conversation (pre-flight detections, three branches keyed on prior state, tailored questions, show-what-will-run confirmation); the side effects all go through `kunskap setup`. Locked by `tests/contract/setup-slash.bats` which asserts the frontmatter shape, the body mentions `kunskap setup`, all three branches (A: fresh, B: existing identity, C: already-enabled) are covered, and the four pre-flight probes are documented (`command -v kunskap`, `kunskap whoami`, `.claude/kunskap.json`, `$ARGUMENTS`).
- **Three branches keyed on pre-flight state.** Branch A (fresh user, no identity, no marker): asks identity + vault + multiplayer; suggests `whoami`+`hostname -s` as defaults so most users type ≤2 short replies. Branch B (existing identity, no marker): skips identity, asks vault + multiplayer. Branch C (project already enabled): reads the existing marker and offers re-confirm / change-vault / disable / cancel. Pre-flight check 1 (`command -v kunskap`) catches the post-`/plugin install` "did `/reload-plugins` run?" case with an actionable error rather than a confusing "command not found".
- **`--init` mode commits the scaffold as a single baseline commit** so `kunskap learn status` reports `INBOX: 0 uncommitted notes` immediately after setup. Without this, the seeded example notes from `kunskap init` would surface as a phantom "uncommitted" backlog on the first status run, falsely suggesting the user needs to `/kunskap:sync`. The commit author is the caller's identity-string. For multiplayer + shared vaults that already have an `origin` configured, push is best-effort — failure prints `setup: scaffold committed locally; push failed` with an actionable stderr hint and exits 0; for shared vaults with no remote yet (Pass-1-review edge case), push is silently skipped.
- **Risk #7 (vault-leak privacy) discipline preserved.** `--yes` is opt-in only and propagates to every destructive step; without it, `kunskap setup` refuses at the first interactive prompt rather than silently auto-confirming. Locked by the `tests/contract/setup-cli.bats` propagation case which runs setup with `</dev/null` and asserts the marker is NOT written and the message contains `cancelled`. The CLI's existing identity-overwrite refusal is also exercised: passing `--name`/`--host` that differ from an existing identity refuses without `--yes`, prints both the old and new strings, and tells the user how to confirm.
- **`set_role_primary` helper (tightly scoped TOML mutation).** Rewrites a `primary = "..."` line within `[roles.<role>]` via awk + temp file + atomic rename. Errors with named exit codes if the section header is missing (vault wasn't `kunskap init`'d) or if no `primary =` line is present (roles.toml hand-broken). Used only by `--multiplayer`; full TOML mutation is explicitly out of scope (deferred to whatever needs it). Idempotent — overwrites whatever value is there (TBD or already-set).
- **CI smoke — branch A end-to-end** (`.github/workflows/ci.yml`): a single `kunskap setup --name vigil --host runner --vault $tmp --init --multiplayer --yes` invocation, then byte-level assertions on the resulting state (identity file, two `primary = "vigil@runner"` lines in roles.toml, single baseline commit, clean working tree, project marker exists, CLAUDE.md managed block injected). Catches the failure modes that unit-style bats can miss: missing CLAUDE.md inject, accidental double-commit, dirty working tree at end-of-setup, push side effects on a non-shared vault.
- **Pass-2 review fixes (ops realism).** Codex Pass 2 caught two pre-merge risks that the unit-style bats coverage missed: (1) `set_role_primaries` was using `mktemp -t kunskap-roles.XXXXXX`, which lands in `TMPDIR` (`/var/folders/...` on macOS) — typically a different filesystem from the vault. Cross-fs `mv` falls back to copy+unlink and is no longer atomic; under disk pressure or permission changes mid-copy, you'd get a half-written `roles.toml`. Fix: place the tempfile next to the target via `mktemp "$file.XXXXXX"` so `mv` becomes a same-fs `rename(2)` — actually atomic. Locked by a bats grep that asserts the new form is present and the bare `-t` form isn't. (2) The naive `[[ -d "$vault/.git" ]]` check (used in three places: rebase guard, the multiplayer warning, the commit-and-push block) skips git worktrees — `.git` is a *file* (not a directory) in `git worktree add`'d checkouts, pointing at the worktree's gitdir. Fix: switch to `git -C "$vault" rev-parse --is-inside-work-tree`, the canonical detection helper used by `cmd_lint` (line 917). Verified empirically: `.git` in a worktree shows as "Regular File", naive `[[ -d ]]` returns false, but `rev-parse --is-inside-work-tree` correctly returns "true" — and `kunskap setup --multiplayer --yes` now correctly commits + pushes from inside a worktree. Locked by a bats grep that asserts the canonical form is present and the naive `[[ -d "$vault/.git" ]]` form isn't. Branch B's identity-resolution rules also extended to accept `"yes"` as a synonym for `"change"` — users naturally answer "yes" to "Change?".
- **Pass-1 review fixes.** Codex Pass 1 caught a critical shell-semantics bug in `set_role_primary`: the `if ! awk ...; then rc=$?` form captures the exit status of the `!` inversion (always 0), not awk's exit code, so the named exit codes 2 (missing section) / 3 (missing primary line) were dead. Fix: switch to `awk ... > "$out" || rc=$?` and dispatch on `$rc` via a `case` — the named exit codes now actually drive the user-facing error message. Also folded the two-pass `set_role_primary` (called twice — once per role) into one `set_role_primaries` that walks the file once, making the mutation atomic (curator + linter either both update or neither does — no half-state on partial failure). Added a `mid_rebase_guard` call at the top of `cmd_setup` for the not-`--init` path so a wedged vault refuses with an actionable message before any mutation. Locked all three by new bats cases: `set_role_primaries reports a clear error when [roles.curator] section is missing` (would have failed under the old `if !` form because rc was always 0 → fell through to "awk rc=0" generic), `set_role_primaries reports a clear error when a [roles.X] section is missing its primary line`, `set_role_primaries is atomic — partial failure leaves roles.toml unchanged` (sha-compares before/after), `setup refuses to mutate a vault that's mid-rebase`, and the composition-lock test extended to require `mid_rebase_guard` in the `cmd_setup` body.
- **`commands/setup.md` Branch B "Change identity?" path tightened.** Pass-1 review flagged that the original wording (`Change? (no = continue with this identity)`) didn't define the yes-path mechanics. New version explicitly enumerates the recognized vocabulary across all three questions (`ok` / `default` / `keep` / blank-line / Enter / `solo` / `multiplayer` / `use <path>` / `init <path>`), specifies the change-identity flow (re-asks Branch A's identity question, requires `--yes` because the CLI refuses identity-overwrite without it), and adds a fallback rule for off-grammar replies (reprint once; default to safest after two unrecognized inputs). Reduces the chance a real Claude session wanders past the ≤10-turn budget on edge-case user replies.
- **bats coverage extended.** `tests/contract/setup-cli.bats` (CLI surface — every flag combination, three identity-state branches, three vault-state branches, multiplayer roles.toml mutation against fresh and existing vaults, shared-vault-no-remote silent-skip path, --yes propagation discipline, `KUNSKAP_AUTO_CONFIRM=1` fallback, mid-rebase guard, named-exit-code surfacing for both error modes of `set_role_primaries`, atomicity check, the composition lock). `tests/contract/setup-slash.bats` (frontmatter shape, allowed-tools restricted to Bash + Read, body mentions `kunskap setup`, all three branches covered, the four pre-flight probes documented, conversation principles surfaced — show-what-will-run, verbatim-CLI-output, ≤10-turn budget — README cross-reference). Both files wired into `ci.yml`.
- **README onboarding section collapsed to `/kunskap:setup`** as the recommended first action after `/plugin install`. The four standalone primitives are still documented for users who'd rather wire by hand. Roadmap checklist updated to mark v1.2 done; `kunskap migrate` (import existing markdown notes) called out as a v1.3+ candidate.
- `bin/kunskap version` bumps to `1.2.0`. Plugin manifest version bumps to `1.2.0`.

## v1.1.1 — 2026-05-06 — status copy + init panel + CI consolidation

- **`kunskap learn status` clarifies "no primary configured" case.** When both curator + linter primaries are still `TBD` (the on-disk default scaffolded by `kunskap init`), the role-check now reads `ROLE-CHECK: no primary configured (curator and linter primaries are TBD; edit _meta/roles.toml to declare)` instead of the misleading pre-v1.1.1 wording `not primary (curator: TBD; linter: TBD)`. The latter implied the caller failed an identity check against a *configured* primary; in fact no primary was set. New mixed-state branches: when one role is configured and the other is `TBD`, the output names them individually (e.g. `not primary (curator primary: sebastian@laptop; linter primary is TBD)` or, when identity matches the configured one, `primary for curator (linter primary is TBD; edit _meta/roles.toml to declare)`). The neither-TBD wording is unchanged.
- **`kunskap init` next-steps panel explains role config WHY + HOW.** Adds a tight paragraph after the existing "Edit `_meta/roles.toml`" line: WHY — the single-primary contract prevents write conflicts on shared vaults (one machine curates, one lints; everyone reads); solo / single-machine users can skip, defaults are fine. HOW — `kunskap whoami` prints your slug; set `roles.curator.primary` and `roles.linter.primary` in `_meta/roles.toml`, then `git add _meta/roles.toml && git commit -m "roles: assign primaries" && git push` so teammates pick it up. Targets the friction window where a fresh user finishes init, sees "Edit roles.toml", and has no idea whether it's optional or what it does.
- **CI workflow consolidation: `p0-p6.yml` → `ci.yml`.** The seven phase-named workflows from the P0–P6 chain are chain artifacts; v1.0+ contributors don't benefit from per-phase split. Consolidated into a single `ci.yml` with three jobs by surface: `manifest` (the P0 surface — manifest schema validation + `bin/kunskap` argparse contracts; no bats install), `shellcheck` (cumulative — `bin/kunskap` + every hook script), and `contract` (every bats suite, every e2e flow, every agent / command / template shape check, every drift lint, the static-prompt fingerprint substring-collision detector). The README CI badge was repointed at `ci.yml`. Drift lint extended: the live `README.md` must not reference any retired `workflows/p[0-6].yml` (historical CHANGELOG entries that name them are legitimate and exempt). New contributors now see one CI workflow on a PR instead of seven phase workflows; coverage is byte-equivalent (every job in every prior `pN.yml` was walked through and reproduced before deletion).
- **Test coverage.** `tests/contract/learn-cli.bats` extended with three role-state cases that lock the new wording: both-TBD, one-TBD-one-configured-not-matching, and one-TBD-one-configured-matching. `tests/contract/init-cli.bats` extended with a panel-content case asserting the WHY phrase ("single-primary contract"), the HOW slug-discovery hint (`kunskap whoami`), and the commit-and-push trio (`git add` + `commit` + `push`). No new test files; the existing 395+ contract suite plus these new cases is the regression check on the CI consolidation — if `ci.yml` had dropped any prior `pN.yml` job, the consolidation PR's first CI run would have surfaced it red.
- `bin/kunskap version` bumps to `1.1.1`. Plugin manifest version bumps to `1.1.1`.

## v1.1.0 — 2026-05-06 — capture + recall: closing the value loop

- **`templates/LAUNCH_FOOTER.md` portable convention sheet.** Single source of truth for capture + recall conventions — inbox-note format, source-priority rule, capture trigger, recall trigger, rules block. Lifted from the conductor's external `LAUNCH_FOOTER.md` so the conventions live inside the plugin and travel with installs.
- **CLAUDE.md injection on `kunskap learn enable` (P0 of the v1.1 value prop).** Adds a managed block bounded by `<!-- BEGIN/END kunskap (managed) -->` to the project's `CLAUDE.md` (creating the file if needed) carrying the LAUNCH_FOOTER body plus a Recall addendum: *"At the start of every new task, run `/kunskap:recall <keywords>` to surface prior learnings + research relevant to the goal."* Every session in a kunskap-enabled project sees the contract — no dependence on conductor-launched footers or description-triggered Skill auto-load. Idempotent: re-enable replaces the block in place. Tamper detection: the block carries a `<!-- kunskap:hash <sha256> -->` line; `learn disable` recomputes the hash and refuses if the body has been hand-edited inside the fence (no silent nuking of user edits). User content surrounding the block is preserved on both enable and disable.
- **`/kunskap:learn disable` round-trip cleanly removes the block.** When the managed-block hash matches, the block is excised (and the CLAUDE.md file removed entirely if the block was the only content). Manual-edit refusal exits 1 with an actionable stderr message; the marker stays put so the user can review and re-disable after manually cleaning up.
- **Per-write async sync via `PostToolUse(Write|Edit)` hook.** New `hooks/post-write-sync.sh` fires on every Write/Edit and (after path-prefix + traversal checks) commits + pushes inbox writes asynchronously. `async: true` in `hooks.json` per the canonical hooks reference; the script also self-backgrounds so the agent never blocks on git or network even if a Claude Code version doesn't honour `async`. Closes the SIGKILL/SIGHUP loss window left by the v1.0 SessionEnd hook (which silently dropped unpushed commits when the process died).
- **`SessionEnd` hook removed.** The per-write hook covers the use case more reliably (catches kill-9, SIGHUP, IDE-managed lifecycles where SessionEnd never fired). Net code-surface decrease.
- **`/kunskap:sync` slash command + `bin/kunskap sync` CLI.** Manual flush for the rare retry case (push failed earlier; user wants to deliberately flush before disconnecting). Loud about failures — exit 1 on push fail with clear stderr — opposite of the silent async hook, on purpose. Solo vaults exit 0 with a "not shared" message.
- **`kunskap learn status` surfaces uncommitted-inbox count.** New `INBOX:` line shows `0 uncommitted notes` when clean or `N uncommitted notes — run /kunskap:sync (or kunskap sync) to flush` when non-zero. Free visibility into pending captures.
- **`SessionStart` passive uncommitted nudge.** After the `git pull --rebase`, the hook prints a one-line warning to stderr if the inbox has uncommitted notes from a prior session: *"⚠ N uncommitted inbox note(s) from prior session — run /kunskap:sync to push."* No new hook firing in the hot path; one extra `git status` per session start.
- **Recall on task start (CLAUDE.md instruction).** The injected CLAUDE.md tells the agent to run `/kunskap:recall <keywords>` at the start of every non-trivial task. Closes the read-side of the value loop. Future direction (v1.2+): plan-mode hook for forced recall, embedding-based recall ranking.
- **`templates/inbox-note.md` enriched to LAUNCH_FOOTER parity.** Filename pattern, frontmatter shape, source-priority rule, two example bodies (a real `learning` and a real `idea`), entry format for both learnings and improvements, rules block. Replaces v1.0's 12-line stub.
- **`skills/kunskap-vault/` removed.** Was a v0.0.1 stub that never landed real content. CLAUDE.md injection covers the same role with deterministic visibility; auto-load-by-description proved unreliable in practice (Sebastian's first end-to-end smoke surfaced the gap).
- **README updates.** Flow section reframed as agents-first capture (was "you write inbox notes; hooks sync them" — that put humans first; the value prop is agents auto-capturing). Four-flow model (capture, sync, curate, recall) replaces the three-flow model. CLAUDE.md injection + per-write async sync + auto-recall documented. Skill mentions removed silently. Mermaid diagram updated.
- **`docs/kunskap-design.md` updates.** v1.1 deltas in the Decisions Locked block (#4 distribution model, #8 sync, #10 recall). Q6 hooks rewritten for the v1.1 model (PostToolUse + post-write-sync.sh sketch + manual lever). New Q9 §Recall trigger covering the CLAUDE.md self-instruction model + tamper detection + idempotency + future directions. Skill references removed.
- **bats coverage extended.** New contract suites: `tests/contract/launch-footer-template.bats` (template invariants), `tests/contract/learn-enable-claudemd.bats` (CLAUDE.md inject + disable round-trip, garbled-markers refusal, manual-edit refusal, missing-fingerprint refusal, idempotency, stale-block replacement, surrounding-content preserve, byte-exact round-trip, whitespace-corrupted markers refusal), `tests/contract/post-write-sync.bats` (happy-path push to bare remote, non-inbox no-op, traversal rejection, non-canonical marker tolerance, solo-vault skip, rebase guard, push-failed soft, empty-change no-op, missing-identity silence, malformed-stdin tolerance), `tests/contract/sync-cli.bats` (sync happy/no-op/refusal paths). `learn-cli.bats` extended with INBOX visibility-nudge cases. `hooks-smoke.bats` updated: SessionEnd assertions removed, PostToolUse shape + Write|Edit matcher + `async: true` + SessionEnd-removal asserted, session-start passive-nudge cases added.
- `bin/kunskap version` bumps to `1.1.0`. Plugin manifest version bumps to `1.1.0`.

## v1.0.1 — 2026-05-06 — curator + linter subprocess permission fix

- **`bin/kunskap curate` + `bin/kunskap lint` spawn shape.** Switched from `--permission-mode acceptEdits` to `--permission-mode dontAsk` paired with an explicit `--allowed-tools` allow-list at both spawn sites. Per canonical Claude Code permission docs (§Permission modes), `acceptEdits` auto-approves Edit/Write and a fixed set of filesystem commands (`mkdir`, `mv`, `cp`, etc.) but does NOT cover `Bash` git operations; in headless mode an unmatched git op falls through to a `canUseTool` callback that doesn't exist, so the agent aborts cleanly per its contract but never completes the work. `dontAsk` converts any unmatched tool into an explicit denial; the allow-list pre-approves exactly the tools each agent prompt invokes — no broader.
- **Curator allow-list (atomic per-article-commit MUST 1 unblocked).** `Read,Write,Edit,MultiEdit,Glob,Grep,Agent` + `Bash(git {status,diff,log,rev-parse,ls-files,check-ignore}:*)` (read-only) + `Bash(git {add,commit,mv,rm}:*)` (mutations) + `Bash({mkdir,jq,tee,date,grep,sort,uniq,kunskap}:*)` (run-record recipe + stub-discovery pipeline). Bare `git foo` form only — the cd-into-vault before spawn makes CWD = vault, so the agent's bare form is sufficient. `git push` is intentionally absent — the SessionEnd hook owns push (P2). `Bash(*)` is intentionally absent — that would be bypass-equivalent.
- **Linter allow-list (read-only invariant tightened).** Same shape as curator's but TIGHTER: no `Edit` / `MultiEdit` (the linter never modifies existing files); no `Bash(git {add,mv,rm}:*)` curator-style rules; instead a per-path exact match `Bash(git add _meta/last-run/linter.json)` for the run-record write — the SOLE permitted exception per linter MUST 8 + MUST 9. Plus `Bash({awk,find,xargs}:*)` for the canonical anchor-to-NR==1 frontmatter parsing recipe in `agents/linter.md` §Frontmatter parsing (MUST 3 staleness walk; the first smoke didn't catch this because the smoke vault had no stale drafts). The CLI authoritative invariant check (`status_before/after` + HEAD oid + `git log --name-only` enumeration, P4 §3) remains the trust boundary; the per-path allow rule is defense in depth.
- **Pass-2 empirical finding (`*` is a literal asterisk, not a glob).** During the dev dance, an end-to-end probe of `claude 2.1.129` revealed that `*` inside `--allowed-tools` matchers is a LITERAL character — `Bash(git -C * foo:*)` does NOT match `git -C /abs/path foo`. An earlier draft of this fix included redundant `Bash(git -C * <op>:*)` rules in both lists; binary-vs-docs check showed they were dead weight and they were dropped. Repros also confirmed `>` shell redirection defeats per-tool prefix matching (`Bash(echo:*)` does not match `echo foo > /tmp/x`) but `|` does not — so the linter directive prompt instructs the agent to use `jq -n ... | tee <path>` instead of `jq -n ... > <path>`. `--permission-mode dontAsk` is a canonical enum value alongside `acceptEdits`/`auto`/`bypassPermissions`/`default`/`plan` (visible in `claude --help`).
- **Real-usage finding 2026-05-06.** Surfaced by the first end-to-end smoke test against a fresh vault. Both agents aborted cleanly per their contracts (no half-state, vault byte-identical to pre-run — discipline held) but neither could complete the work. Ships the load-bearing `kunskap curate` flow that v1.0 packaged but couldn't actually run.
- **bats coverage extended.** New spawn-shape assertions in `tests/contract/role-cli.bats`: `--allowed-tools` + `--permission-mode dontAsk` present, `acceptEdits` regression caught, `bypassPermissions` and `Bash(*)` rejected (defense-in-depth against `/simplify` swapping to bypass for convenience), curator covers `add`/`commit`/`mv`/`rm` + `mkdir`/`jq`, curator omits `git push`, linter is provably tighter than curator (per-path `add`, no broad `git add:*`), linter covers MUST-3 frontmatter parsing (`awk`/`find`/`xargs`) and MUST-6 read-only git ops, both include `Agent` for subagent invocation, neither uses the dead `Bash(git -C * ...)` form (Pass-2 lock).
- `bin/kunskap version` bumps to `1.0.1`. Plugin manifest version bumps to `1.0.1`.

## v1.0.0 — 2026-05-05 — P6 search + polish + marketplace (v1.0 release)

- **`commands/recall.md` + `bin/kunskap recall <query> [--vault <abs-path>] [--author X] [--tag Y] [--limit N] [--format text|json]`.** Slash and CLI surface for searching the vault from inside a Claude Code session. Identity-independent — works on any machine regardless of role (P5 §8: read-only operations are orthogonal to identity gates). Vault resolves from `--vault` flag, then the project marker (`.claude/kunskap.json`), same shape as `lint` and `drafts`.
- **Two-stage search.** Stage 1 (load-bearing per design §Q8): `rg --json --type md --fixed-strings` over `<vault>/wiki/` + `<vault>/raw/inbox/`; jq normalizes paths to vault-relative form. Stage 2 (best-effort): `obsidian search query="..." format=json` reranks paths by Obsidian's relevance score. Detection chain: `command -v obsidian`, fallback to `/Applications/Obsidian.app/Contents/MacOS/obsidian-cli` on macOS, then `pgrep -x Obsidian`, then a `version`-call smoke (the binary exists even when the CLI toggle is off; the smoke catches that). When any check fails, Stage 1 carries silently.
- **Risk #2 (Obsidian CLI partially unverified) closed.** The design's `obsidian search query="..." format=json` syntax is verified live against `obsidian.md/help/cli` (active page, not a 404 anymore). The smoke chain hits real Obsidian when the CLI is enabled in the user's Settings; otherwise degrades cleanly.
- **Filters + scoring.** `--author X` exact match against frontmatter `author:` field; `--tag Y` membership match against frontmatter `tags:` (inline or block list — anchor-to-NR==1 awk per P3 §1+§2). Score = file hit-count, with Obsidian's relevance rank as the primary sort dimension when Stage-2 ran. `--limit N` truncates after sort (default 20). Sort tie-breakers: path then line number for determinism.
- **Output shapes.** Default text mode: bulleted snippets (`- path:line (score N) [tags]\n    snippet`). `--format json`: `{hits: [{path, line, snippet, score, author, tags, obs_rank}]}` for agent consumption. Exit codes: 0 = at least one hit, 1 = no hits (same in both modes — shell consumers can pipeline).
- **`templates/vault-init/wiki/bases/`.** Three starter Obsidian Bases for human-side queries: `by-author.base` (group articles by author), `recent.base` (table sorted by `last_curated`), `draft-staleness.base` (drafts by age). Bases syntax verified against `obsidian.md/help/bases/syntax`. Neutral content per Risk #7 — no project-specific filters.
- **`README.md` final polish.** Quickstart now uses the marketplace (`/plugin marketplace add Abeansits/kunskap-marketplace`); three core flows (capture, curate, recall); Risks #5 / #7 / #8 surfaced inline; status reads v1.0.
- **`Abeansits/kunskap-marketplace` repo.** New public repo carrying `.claude-plugin/marketplace.json` (per the canonical schema at `code.claude.com/docs/en/plugin-marketplaces`) that lists the kunskap plugin with `source: github` pointing at `Abeansits/kunskap`. Plugin repo + marketplace repo are content-free and public; vault repos remain private (Risk #8).
- **CI `.github/workflows/p6.yml`.** bats coverage for recall (query, `--author`, `--tag`, `--limit`, `--format json`, empty result, read-only invariant, Stage-2 graceful skip). Marketplace.json shape lint. Full prior-phase regression suite. Drift lints (P1 §7, P4 §7, P4 §9) extended to the new surface.
- `bin/kunskap version` bumps to `1.0.0`. Plugin manifest version bumps to `1.0.0`.

## v0.5.0 — 2026-05-05 — P5 role assignment + sandbox fix

- **`_meta/roles.toml` enforcement.** `bin/kunskap curate` and `bin/kunskap lint` refuse to run unless `kunskap whoami` matches `roles.<role>.primary`, except with `--force` (interactive confirmation; pass `--yes` or set `KUNSKAP_AUTO_CONFIRM=1` in scripts; recorded as `forced=true` in `_meta/last-run/<role>.json` for audit trail). Single-primary, no fallback, no advisory lock — design §Q4 explicitly rejected the lock variant after a TOCTOU finding in pass-2 review. Missing or `primary="TBD"` warns and proceeds (solo and freshly-init'd vaults must work without role config).
- **CWD-fix for headless agents (P4 §2 deferred).** `kunskap curate` and `kunskap lint` now `cd "$vault"` and pass `--add-dir "$vault"` before spawning `claude --plugin-dir`, so the agent's filesystem sandbox covers the vault from any working directory. Without this, running `kunskap curate --vault X` from any project dir other than `X` produced sandbox-blocked agent reads (manifesting as `[DRIFT]` self-check errors on the linter side and partial curator runs).
- **`_meta/last-run/{curator,linter}.json` recording (sharded per-role).** Curator agent writes `_meta/last-run/curator.json` on every successful run (`ran_at`, `by`, `inbox_processed`, `articles_written`, `drafts_routed`, `head_before`, `head_after`, `forced`); linter agent writes `_meta/last-run/linter.json` (`ran_at`, `by`, `findings_count`, `forced`). Sharded per-role so curator and linter never write the same file — preserves design §Q4's "no shared file both are racing on" invariant under multi-actor and hand-off scenarios (Codex Pass-2 found that a single shared `_meta/last-run.json` with jq-merge would conflict on rebase when two machines wrote different keys to the same file). The linter's write is the SOLE explicit exception to the read-only invariant (linter MUST 8 + MUST 9); the linter never touches `_meta/last-run/curator.json`. Atomicity: the run-record commit only fires on a clean run; partial / crashed runs leave no record, so the linter's `[CURATOR-IDLE]` clause naturally fires on the next vault audit.
- **CLI authoritative invariant whitelist (P5).** `bin/kunskap lint` enumerates every path touched between pre- and post-snapshots (commits since `head_before` AND new working-tree porcelain entries) and refuses any path that is not `_meta/last-run/linter.json`. Exit code 2 with a stderr listing of disallowed paths. Closes the gap between the linter's whitelisted single-write and the read-only contract (P4 §3 head-oid + porcelain pair generalized).
- **Preflight ordering (P1 §5 self-state-before-external).** `curate` and `lint` self-state checks fire in order: vault structure → agent-file → mid-rebase guard (P4 §4 reapplied) → identity (`whoami` exits 0) → role check → claude-on-PATH. Identity-not-set produces a clear "set identity first" message before any external dependency check.
- **TOML reader: documented constrained subset.** `bin/kunskap`'s awk parser stays — single-level table headers, double-quoted strings, no inline tables / arrays / multi-line strings / nested dotted-keys. The subset is documented in `docs/ARCHITECTURE.md` and locked by `tests/contract/toml-fuzz.bats`: subset-conforming inputs round-trip; out-of-subset inputs fail-soft (no crash, undefined value).
- **Two-machine identity simulation in tests.** `tests/contract/two-machine.bats` swaps `XDG_CONFIG_HOME` between two distinct `identity.toml` files against the same vault; asserts machine-A (matches `roles.curator.primary`) succeeds, machine-B (mismatch) refuses with `--force` suggestion, machine-B + `--force --yes` proceeds with override stderr.
- **CI lints (P4 ✨ deferreds).** Static-prompt fingerprint substring-collision detector scoped per agent (curator vs linter) — locks the discipline that each `*_prompt_contains` fingerprint is unique to its clause (P4 §7). CHANGELOG/README numeric-fact drift detector — fails on `\b<int>\s+(MUST|tests?|cases|bats)\b` (P4 §9 generalization of the P1 §7 hardcoded-test-count lint).
- **`README.md` documents the role hand-off procedure** + the multi-machine constraint at the top of "Status".
- `bin/kunskap version` bumps to `0.5.0`.

## v0.4.0 — 2026-05-05 — P4 linter agent

- `agents/linter.md` — read-only health-check agent (Karpathy "Linting"). Frontmatter: `model: opus`, `tools: Read, Bash, Glob, Grep`, `disallowedTools: WebFetch, WebSearch, Write, Edit`. The Write/Edit denial is the read-only invariant in code: the curator owns the wiki, the linter only audits.
- `commands/lint.md` + `bin/kunskap lint [--vault <abs-path>] [--format text|json]` — vault resolves from `--vault`, then the project marker (`.claude/kunskap.json`). Spawns the agent headlessly via `claude --plugin-dir` (P1 §2 lesson — directive prompt naming the agent, not slash invocation).
- Eight MUST clauses for the linter, each fingerprint-tested in `linter-static-prompt.bats`: `[DRIFT]` across articles (MUST 1), `[STUB-CLUSTER]` for 3+-ref orphans (MUST 2 — 2-ref is curator territory), `[DRAFT-STALE]` (≥7d created) + `[DRAFT-DEFERRED-STALE]` (≥14d deferred) (MUST 3), `[OFFLINE-ARRIVAL]` (MUST 4 — Risk #4, author not seen in `_meta/last-run.json#curator.by` for ≥48h), `[CURATOR-IDLE]` (MUST 5 — `ran_at` ≥48h ago or file missing), `[IDENTITY-MISMATCH]` (MUST 6 — committers in `git log --since=30.days.ago` not declared as `roles.<role>.primary`), the `[TYPE] path | message` output contract (MUST 7), and the read-only invariant (MUST 8).
- Read-only invariant enforced **at the CLI boundary**: bin/kunskap snapshots `git status --porcelain --untracked-files=all` AND `git rev-parse HEAD` BEFORE the agent spawn (so a pre-existing dirty vault isn't blamed) and compares post-run. Non-equal porcelain OR a HEAD oid change → distinct exit code 2 with a stderr diff. The HEAD-oid check closes the write+commit bypass; the `--untracked-files=all` flag ensures untracked-dir collapse can't mask a write deep in an existing untracked directory. Worktree-aware via `git rev-parse --is-inside-work-tree`.
- Output format: `[TYPE] path | message` lines grouped by severity in text mode; `--format json` emits a single `{findings: [...]}` object with `{type, severity, path, message, suggested_action}` records sorted by `(severity, type, path, message)` lexicographically (Pass-2 — cron-diff stability) and always exits 0 (the JSON IS the output). Text mode exits 0 when no findings, 1 when findings present (so cron / CI can use the exit code as a vault-health signal).
- Mid-rebase / merge / cherry-pick guard: `kunskap lint` refuses to run when the vault is in `.git/rebase-merge`, `.git/rebase-apply`, has `MERGE_HEAD`, or has `CHERRY_PICK_HEAD` — porcelain shifts during git rewrites would false-positive the read-only invariant check (P2 §3 mid-rebase-wedge lesson). The guard fires before the `claude` PATH check so the error is actionable on hosts without Claude Code installed (P1 §5 self-state-before-external ordering).
- MUST 4 14-day upper bound: `[OFFLINE-ARRIVAL]` only fires for inbox / archived notes whose `created:` is within the last 14 days. Without this bound, a brand-new shared vault that imports historical archives would emit one finding per pre-existing note from any author other than the first curator (Pass-2 finding).
- MUST 8 trust boundary clarified: the CLI's pre/post snapshot of `git status --porcelain --untracked-files=all` + `git rev-parse HEAD` is the authoritative invariant check; the agent's own `git status` self-check is best-effort defense in depth. If the two disagree, the CLI is right (Pass-2 alignment).
- `linter-static-prompt.bats`, `lint-cli.bats`, `linter-behavioral.bats` (gated `KUNSKAP_LIVE_TESTS=1`) — every linter MUST clause maps to a fingerprint test (P1 §1 safety net), every CLI surface (arg parsing, marker fallback, agent-file preflight, symlink shim, exit-code semantics) is exercised, every finding type has a behavioral assertion against a seeded scenario vault.
- Linter system prompt encodes the canonical anchor-to-NR==1 awk recipe for frontmatter parsing (P3 §1+§2 lessons — never use sed range patterns; many `wiki/**/*.md` files reads benefit) and the v0 §6 stub-discovery grep recipe verbatim.
- CI `.github/workflows/p4.yml`: shellcheck, manifest-version drift check, agent + command frontmatter shape, dynamic test-count printout, full bats regression suite, end-to-end `init → lint preflight` smoke (vault remains unmodified on failed preflight — read-only contract holds even when `claude` isn't on PATH).
- `bin/kunskap version` bumps to `0.4.0`.

## v0.3.0 — 2026-05-05 — P3 vault bootstrap + drafts surface

- `bin/kunskap init <path> [--shared <git-remote>] [--name <human-readable>] [--force] [--yes]` (per design §Q3). Self-state checks before external (P1 §5 lesson): validates target before requiring `git` on PATH or the template dir. Existing non-empty target requires `--force` plus interactive confirmation (Risk #7 — labeled `PATH:` / `ENTRIES:` summary so wrong-path mistakes are obvious). `KUNSKAP_AUTO_CONFIRM=1` and `--yes` bypass the prompt for scripts.
- `templates/vault-init/` — full scaffold: `_meta/{kunskap,roles}.toml`, `wiki/{_index.md,_drafts,learnings,ideas,concepts,patterns,bases}/`, `raw/inbox/`, `Archives/{processed-inbox,processed-drafts/{approved,rejected}}/`, `README.md`, `.gitignore`. Substitutes `{{name}}` / `{{created}}` / `{{shared}}` via bash literal-substitution (no sed escaping).
- `templates/article.md` + `templates/inbox-note.md` — version-controllable shape references for the curator and session-end captures.
- Sample notes seeded by `init` are deliberately **neutral** (no specific research domain, per Risk #7 vault-leak prevention) and shaped so a fresh vault passes `kunskap audit-coverage` with `covered_count >= 1`.
- `commands/drafts.md` + `bin/kunskap drafts list | show | approve | reject | defer` — port of a prior `/drafts` surface (proven in solo flow, ported into the plugin per design §Drafts review loop). Vault resolution: `--vault` flag wins, else project marker `.claude/kunskap.json`. `approve [--into <slug>]` either creates a new article or appends under a dated heading; archives original to `Archives/processed-drafts/approved/` with `approved_at` + `approved_into` stamps. `reject` requires `--reason` (rejection without reason loses context). `defer` stamps `deferred_at:` and is idempotent. Each action is a single git commit via `git add -A wiki Archives` (so deletions get staged, not just adds).
- Drafts naming convention locked: curator writes `_drafts/<topic-slug>--<iso-date>.md` (per design §Open decisions #5). Three new static-prompt tests assert the convention is encoded in `agents/curator.md` so future prompt edits can't silently drop it.
- CI `.github/workflows/p3.yml`: shellcheck, version-drift check, help-text covers `init` + `drafts`, templates tree completeness + substitution-placeholder lint, `commands/drafts.md` argument-hint check, full bats suite, end-to-end `init → curate --check → drafts list` smoke (the P3 hard contract: a freshly-init'd vault must satisfy curate-check and drafts-list with no manual fixup).
- `bin/kunskap version` bumps to `0.3.0`.

## v0.2.0 — 2026-05-05 — P2 opt-in surface + sync hooks

- `commands/learn.md` + `bin/kunskap learn enable | disable | status` (per design §Q6).
- `bin/kunskap config get <key>` with new global `--vault <abs-path>` flag — vault context reads `_meta/kunskap.toml [vault]`, user context reads `identity.toml [user]`. Missing `_meta/kunskap.toml` is fail-soft (`shared` returns `false`) so hooks survive pre-init vaults until P3.
- `hooks/hooks.json` + `session-start.sh` + `session-end.sh` + `_shared.sh` — SessionStart pull, SessionEnd commit-and-push for shared vaults only. **Hard fail-soft contract**: every error path exits 0 with a stderr message, never blocks session start/end. Both hooks detect mid-rebase wedge state (`.git/rebase-merge` / `.git/rebase-apply`) and refuse with an actionable message rather than auto-aborting (Codex Pass-2 block-ship).
- `.claude/kunskap.json` marker schema with `confirmed_at` ISO-8601 timestamp; `learn status` warns when confirmation is >30 days old (Risk #7 — vault-leak between projects).
- `learn enable` requires interactive confirmation unless `--yes` or `KUNSKAP_AUTO_CONFIRM=1` (Risk #7 privacy primitive). Confirmation prompt prints labeled `PROJECT:` / `VAULT:` lines so wrong-vault mistakes are obvious (Codex Pass-2).
- `learn status` cross-checks identity against `roles.toml` primaries and reports primary / not-primary (Risk #5 — identity fragility), and surfaces remote health for shared vaults (`REMOTE: <url>` or `REMOTE: not configured` — Codex Pass-2).
- Adversarial inbox fixtures (P1-deferred): two notes whose prose-label contradicts body content; behavioral assertions that the curator routes by policy, not by prose label.
- CI `.github/workflows/p2.yml`: `hooks.json` shape validation, `plugin.json` fuller-than-P0 schema validation (P0-deferred), shellcheck on hooks, dynamic test-count printout (no drift-prone hardcoded numbers per P1 §7), drift lint asserting CHANGELOG / README contain no `(N tests)` strings or retired workflow filenames.
- `bin/kunskap version` bumps to `0.2.0`.

## v0.1.0 — 2026-05-05 — P1 curator agent + manual trigger + tests

- `agents/curator.md` — curator agent encoding the design §Curator contract (Karpathy article-writing model, atomic per-article commits, hand-edit preservation per Risk #3, per-source `## Sources` footers, stub discovery recipe verbatim from v0 §6).
- `commands/curate.md` — `/kunskap:curate <vault-path>` slash command surface.
- `bin/kunskap` adds `curate` (headless agent spawn via `claude --plugin-dir`), `link-stubs` (resolved / alias / needs-stub / orphan classification), and `audit-coverage` (silent-drops + broken-provenance) subcommands.
- Fixture-based contract test suite at `tests/contract/`:
  - `static-prompt.bats` — asserts each §Curator contract MUST/MUST-NOT clause is encoded in the agent prompt; catches prompt drift in CI.
  - `cli-surface.bats` — exercises the new CLI subcommands against a hand-crafted fixture vault at `tests/fixtures/curator-vault/`.
  - `behavioral.bats` — gated on `KUNSKAP_LIVE_TESTS=1`; runs the actual curator and asserts end-to-end outcomes.
- CI: `.github/workflows/p1.yml` runs the contract suite alongside P0's `p0.yml`.

Risks #3 (hand-edit overwrite) and #9 (curator contract testability) are now load-bearing-tested.

## v0.0.1 — 2026-05-04 — P0 scaffold

- Plugin manifest at `.claude-plugin/plugin.json`
- `bin/kunskap` with `version`, `config user`, `config show`, `whoami` subcommands (per design §Q5)
- Stub `skills/kunskap-vault/SKILL.md` and `commands/learn.md` (real bodies in P1/P2)
- README + LICENSE (MIT)
- CI: shellcheck on `bin/kunskap`, `plugin.json` parse + required-field check, `bin/kunskap` smoke tests

Tracking: design doc at [`docs/kunskap-design.md`](docs/kunskap-design.md). Phased rollout P0 → P6.
