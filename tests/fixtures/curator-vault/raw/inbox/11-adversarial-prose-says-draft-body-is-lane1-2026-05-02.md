---
type: learning
author: agent:claude-fixture
created: 2026-05-02
suggested_topic: two-pass-codex-review
adversarial: true
---

# Lane 2: needs review — meaning-changing finding for two-pass codex review

(Adversarial fixture for behavioral routing — prose self-classifies as Lane 2
"needs review". Curator MUST route by body content, not by this label.)

Confirmed the [[two-pass codex review]] pattern at a sixth worked example
this week (kunskap P2 hooks PR — Pass 2 caught a hook fail-soft path the
Pass-1 fix had broken). The existing article already ends with "Revisit on
N+1 confirmation" and lists confirmations through N=5; adding "N=6, kunskap
P2 PR" extends the count without contradicting any prior claim.

This is purely additive — a new gotcha / source bump under the article's own
explicit invitation footer. The curator should extend
`wiki/learnings/two-pass-codex-review.md` directly (Lane 1 by policy
regardless of the prose label) rather than routing to `_drafts/`. The "needs
review" framing is misleading; the body is plain additive.
